export type PlacedModel = {
  id: string;
  name: string;
  url: string;
  thumbnail: string;
  height: number;
  position: [number, number, number];
  instanceId: string;
};

export type Model = {
  id: string;
  name: string;
  url: string;
  thumbnail: string;
  height: number;
}